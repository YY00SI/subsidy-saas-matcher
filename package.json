{
  "name": "subsidy-saas-matcher",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch:subsidies": "tsx scripts/fetch-subsidies.ts",
    "build:data": "tsx scripts/build-dataset.ts",
    "update:all": "npm run fetch:subsidies && npm run build:data",
    "test": "vitest run"
  },
  "dependencies": {
    "astro": "^6.0.8"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "vitest": "^2.1.0"
  }
}
